<UserControl
    x:Class="ObjectCountingExplorer.Controls.InitialView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ObjectCountingExplorer.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Object Counting Explorer Demo" Margin="6" HorizontalAlignment="Center" Style="{ThemeResource HeaderTextBlockStyle}"/>

        <Grid Grid.Row="1">
            <TextBlock x:Name="landingMessage" VerticalAlignment="Center" HorizontalAlignment="Center" TextAlignment="Center" Text="Select a photo to score it against the target project" Style="{StaticResource HeaderTextBlockStyle}"/>
        </Grid>

    </Grid>
</UserControl>
